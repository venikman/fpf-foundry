{
  "name": "fpf-foundry",
  "version": "0.0.0",
  "private": true,
  "dependencies": {},
  "devDependencies": {
    "@biomejs/biome": "^2.3.11",
    "oxlint": "^1.39.0"
  },
  "scripts": {
    "check": "bun develop/scripts/no_js_files_check.ts --all && bun run format:check && bun run lint && bun develop/scripts/unicode_safety_check.ts --all && bun develop/tools/skill/validate.ts --all && bun develop/scripts/skill_inventory_checks.ts",
    "format": "biome format --write develop",
    "format:check": "biome check develop",
    "lint": "oxlint develop --deny-warnings"
  }
}
