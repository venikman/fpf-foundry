{
  "name": "fpf-foundry",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {},
  "devDependencies": {
    "@biomejs/biome": "^2.3.11"
  },
  "scripts": {
    "check": "bun develop/scripts/check.ts",
    "format": "biome format --write develop",
    "lint": "biome lint develop",
    "lint:fix": "biome lint --write develop",
    "test": "bun test"
  }
}
