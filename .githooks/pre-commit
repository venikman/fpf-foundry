#!/usr/bin/env bash
set -euo pipefail

if ! command -v bun >/dev/null 2>&1; then
  echo "ERROR: bun is required to run the pre-commit hook (https://bun.sh)."
  exit 1
fi

bun develop/scripts/no_js_files_check.ts --staged
bun develop/scripts/unicode_safety_check.ts --staged
bun run lint
